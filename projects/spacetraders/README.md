# spacetraders

Placeholder — no infrastructure deployed yet.

When spacetraders needs AWS resources, create the stack here following repo conventions
(see root README): S3 backend key `spacetraders/terraform.tfstate`, `use_lockfile = true`, default
tags with `Project = "spacetraders"`, then add the stack path to `.github/workflows/plan.yml`.
